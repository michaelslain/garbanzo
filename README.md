# Epsilon

**Dev**

```zsh
# install dependencies on server
$ yarn

# install dependencies on client
$ yarn client-install

# run server
$ yarn server

# run client
$ yarn client

# run client and server at the same time
$ yarn dev
```
